---
name: pu
description: Shorthand for /pickup — restore session context from a HANDOFF, cold git history, or resume a conversation. Alias only; delegates to the pickup skill.
---

# /pu — alias for /pickup

This is a shorthand alias, nothing more. Immediately invoke the `pickup` skill via
the Skill tool, passing through the user's arguments verbatim. Do not do any pickup
work here — `pickup` owns the logic.
